<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="ItemImport">
    <resultMap id="ItemImportMap" type="ItemImport">
        <id column="id" property="id"/>
        <result column="shop_id" property="shopId"/>
        <result column="item_uploaded_json" property="itemUploadedJson"/>
        <result column="status" property="status"/>
        <result column="error_result" property="errorResult"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        
    </resultMap>

    <sql id="tb">
        vega_item_imports
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        shop_id, item_uploaded_json, status, error_result, created_at, updated_at
    </sql>

    <sql id="vals">
        #{shopId}, #{itemUploadedJson}, #{status}, #{errorResult}, now(), now()
    </sql>

    <sql id="queryCondition">
        where 1 = 1
        
        <if test="shopId != null">and `shop_id` = #{shopId}</if>
        
        <if test="itemUploadedJson != null">and `item_uploaded_json` = #{itemUploadedJson}</if>
        
        <if test="status != null">and `status` = #{status}</if>
        
        <if test="errorResult != null">and `error_result` = #{errorResult}</if>
        
        <if test="createdAt != null">and `created_at` = #{createdAt}</if>
        
        <if test="updatedAt != null">and `updated_at` = #{updatedAt}</if>
        
        <if test="startAt != null">and created_at &gt; #{startAt}</if>
        <if test="endAt != null">and created_at &lt; #{endAt}</if>
    </sql>

    <sql id="order">
        order by id desc
    </sql>

    <insert id="create" parameterType="ItemImport" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="findById" parameterType="long" resultMap="ItemImportMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

   <select id="findByIds" parameterType="list" resultMap="ItemImportMap">
        select id, <include refid="cols_exclude_id"/>
        from <include refid="tb"/>
        where id in
        <foreach collection="list" open="(" separator="," close=")"
                 item="item">
            #{item}
        </foreach>
        <include refid="order" />
    </select>

    <update id="update" parameterType="ItemImport">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="shopId != null">shop_id = #{shopId},</if>
            <if test="itemUploadedJson != null">item_uploaded_json = #{itemUploadedJson},</if>
            <if test="status != null">status = #{status},</if>
            <if test="errorResult != null">error_result = #{errorResult},</if>
            
            
            
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <select id="paging" parameterType="list" resultMap="ItemImportMap">
        select id, <include refid="cols_exclude_id"/>
        from <include refid="tb"/>
        <include refid="queryCondition"/>
        order by id desc limit #{offset},#{limit}
    </select>

    <select id="count" parameterType="list" resultType="long">
        select count(1)
        from <include refid="tb"/>
        <include refid="queryCondition"/>
    </select>

</mapper>