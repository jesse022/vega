<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2018 杭州端点网络科技有限公司, Code generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="VegaItemSync">
    <resultMap id="VegaItemSyncMap" type="VegaItemSync">
        <id column="id" property="id"/>
        <result column="channel" property="channel"/>
        <result column="type" property="type"/>
        <result column="status" property="status"/>
        <result column="sync_id" property="syncId"/>
        <result column="sync_pid" property="syncPid"/>
        <result column="sync_name" property="syncName"/>
        <result column="sync_log" property="syncLog"/>
        <result column="extra_json" property="extraJson"/>
        <result column="user_id" property="userId"/>
        <result column="user_name" property="userName"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        
    </resultMap>

    <sql id="tb">
        vega_item_sync
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        channel, type, status, sync_id, sync_pid, sync_name, sync_log, extra_json, user_id,
        user_name, created_at, updated_at
    </sql>

    <sql id="vals">
        #{channel}, #{type}, #{status}, #{syncId}, #{syncPid}, #{syncName}, #{syncLog}, #{extraJson}, #{userId}, #{userName},
        now(), now()
    </sql>

    <sql id="queryCondition">
        where 1 = 1

        <if test="channel != null">and `channel` = #{channel}</if>
        
        <if test="type != null">and `type` = #{type}</if>
        
        <if test="status != null">and `status` = #{status}</if>

        <if test="syncId != null">and `sync_id` = #{syncId}</if>

        <if test="syncPid != null">and `sync_pid` = #{syncPid}</if>

        <if test="userId != null">and `user_id` = #{userId}</if>
        
        <if test="startAt != null">and created_at &gt; #{startAt}</if>

        <if test="endAt != null">and created_at &lt; #{endAt}</if>
    </sql>

    <sql id="order">
        order by id desc
    </sql>

    <insert id="create" parameterType="VegaItemSync" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <insert id="creates" parameterType="list">
        INSERT INTO
        <include refid="tb"/>
        (<include refid="cols_exclude_id"/>)
        VALUES
        <foreach collection="list" separator="," item="item">
            (
            #{item.channel},
            #{item.type},
            #{item.status},
            #{item.syncId},
            #{item.syncPid},
            #{item.syncName},
            #{item.syncLog},
            #{item.extraJson},
            #{item.userId},
            #{item.userName},
            now(),
            now()

            )
        </foreach>
    </insert>

    <select id="findById" parameterType="long" resultMap="VegaItemSyncMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

   <select id="findByIds" parameterType="list" resultMap="VegaItemSyncMap">
        select id, <include refid="cols_exclude_id"/>
        from <include refid="tb"/>
        where id in
        <foreach collection="list" open="(" separator="," close=")"
                 item="item">
            #{item}
        </foreach>
        <include refid="order" />
    </select>

    <select id="findByChannelAndTypeAndSyncId" parameterType="VegaItemSync" resultMap="VegaItemSyncMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE channel = #{channel} AND type = #{type} AND sync_id = #{syncId}
    </select>

    <select id="findByChannelAndTypeAndSyncIds" parameterType="map" resultMap="VegaItemSyncMap">
        select id, <include refid="cols_exclude_id"/>
        from <include refid="tb"/>
        where channel = #{channel} AND type = #{type} AND sync_id in
        <foreach collection="syncIds" open="(" separator="," close=")"
                 item="item">
            #{item}
        </foreach>
        <include refid="order" />
    </select>

    <update id="update" parameterType="VegaItemSync">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="status != null">status = #{status},</if>
            <if test="syncName != null">sync_name = #{syncName},</if>
            <if test="syncLog != null">sync_log = #{syncLog},</if>
            <if test="extraJson != null">extra_json = #{extraJson},</if>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="userName != null">user_name = #{userName},</if>
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateByChannelAndTypeAndSyncId" parameterType="VegaItemSync">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="status != null">status = #{status},</if>
            <if test="syncName != null">sync_name = #{syncName},</if>
            <if test="syncLog != null">sync_log = #{syncLog},</if>
            <if test="extraJson != null">extra_json = #{extraJson},</if>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="userName != null">user_name = #{userName},</if>
            updated_at=now()
        </set>
        WHERE channel = #{channel} AND type = #{type} AND sync_id = #{syncId}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <select id="paging" parameterType="list" resultMap="VegaItemSyncMap">
        select id, <include refid="cols_exclude_id"/>
        from <include refid="tb"/>
        <include refid="queryCondition"/>
        order by id desc limit #{offset},#{limit}
    </select>

    <select id="count" parameterType="list" resultType="long">
        select count(1)
        from <include refid="tb"/>
        <include refid="queryCondition"/>
    </select>

</mapper>
