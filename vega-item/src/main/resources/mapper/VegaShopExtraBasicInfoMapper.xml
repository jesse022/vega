<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2017 杭州端点网络科技有限公司, Code generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="VegaShopExtraBasicInfo">
    <resultMap id="VegaShopExtraBasicInfoMap" type="VegaShopExtraBasicInfo">
        <id column="id" property="id"/>
        <result column="shop_id" property="shopId"/>
        <result column="shop_name" property="shopName"/>
        <result column="shop_status" property="shopStatus"/>
        <result column="shop_type" property="shopType"/>
        <result column="user_id" property="userId"/>
        <result column="user_name" property="userName"/>
        <result column="mac_address" property="macAddress"/>
        <result column="background_picture" property="backgroundPicture"/>
        <result column="detail" property="detail"/>
        <result column="videos_json" property="videosJson"/>
        <result column="extra_json" property="extraJson"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        
    </resultMap>

    <sql id="tb">
        vega_shop_extras_basic_info
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        shop_id, shop_name, shop_status, shop_type, user_id, user_name,
        mac_address, background_picture, detail, videos_json, extra_json, created_at, updated_at
    </sql>

    <sql id="vals">
        #{shopId}, #{shopName}, #{shopStatus}, #{shopType}, #{userId}, #{userName},
        #{macAddress}, #{backgroundPicture}, #{detail},  #{videosJson},  #{extraJson},
        now(), now()
    </sql>

    <sql id="queryCondition">
        where 1 = 1

        <if test="shopId != null">and `shop_id` = #{shopId}</if>

        <if test="userId != null">and `user_id` = #{userId}</if>
        
        <if test="macAddress != null">and `mac_address` = #{macAddress}</if>
        
        <if test="credit != null">and `credit` = #{credit}</if>
        
        <if test="createdAt != null">and `created_at` = #{createdAt}</if>
        
        <if test="updatedAt != null">and `updated_at` = #{updatedAt}</if>
        
        <if test="startAt != null">and created_at &gt; #{startAt}</if>
        <if test="endAt != null">and created_at &lt; #{endAt}</if>
    </sql>

    <sql id="order">
        order by id desc
    </sql>

    <insert id="create" parameterType="VegaShopExtraBasicInfo" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="findById" parameterType="long" resultMap="VegaShopExtraBasicInfoMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

   <select id="findByIds" parameterType="list" resultMap="VegaShopExtraBasicInfoMap">
        select id, <include refid="cols_exclude_id"/>
        from <include refid="tb"/>
        where id in
        <foreach collection="list" open="(" separator="," close=")"
                 item="item">
            #{item}
        </foreach>
        <include refid="order" />
    </select>

    <select id="findByShopId" parameterType="long" resultMap="VegaShopExtraBasicInfoMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE shop_id = #{shopId}
    </select>

    <select id="findByMacAddress" parameterType="String" resultMap="VegaShopExtraBasicInfoMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE mac_address = #{macAddress}
    </select>

    <update id="update" parameterType="VegaShopExtraBasicInfo">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="macAddress != null">mac_address = #{macAddress},</if>
            <if test="backgroundPicture != null">background_picture = #{backgroundPicture},</if>
            <if test="detail != null">detail = #{detail},</if>
            <if test="videosJson != null">videos_json = #{videosJson},</if>
            <if test="extraJson != null">extra_json = #{extraJson},</if>
            <if test="shopName != null">shop_name = #{shopName},</if>
            <if test="userName != null">user_name = #{userName},</if>
            <if test="shopStatus != null">shop_status = #{shopStatus},</if>
            <if test="shopType != null">shop_type = #{shopType},</if>
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateByShopId" parameterType="VegaShopExtraBasicInfo">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="macAddress != null">mac_address = #{macAddress},</if>
            <if test="backgroundPicture != null">background_picture = #{backgroundPicture},</if>
            <if test="detail != null">detail = #{detail},</if>
            <if test="videosJson != null">videos_json = #{videosJson},</if>
            <if test="extraJson != null">extra_json = #{extraJson},</if>
            <if test="shopName != null">shop_name = #{shopName},</if>
            <if test="userName != null">user_name = #{userName},</if>
            <if test="shopStatus != null">shop_status = #{shopStatus},</if>
            <if test="shopType != null">shop_type = #{shopType},</if>
            updated_at=now()
        </set>
        WHERE shop_id = #{shopId}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <select id="paging" parameterType="list" resultMap="VegaShopExtraBasicInfoMap">
        select id, <include refid="cols_exclude_id"/>
        from <include refid="tb"/>
        <include refid="queryCondition"/>
        order by id desc limit #{offset},#{limit}
    </select>

    <select id="count" parameterType="list" resultType="long">
        select count(1)
        from <include refid="tb"/>
        <include refid="queryCondition"/>
    </select>

</mapper>
