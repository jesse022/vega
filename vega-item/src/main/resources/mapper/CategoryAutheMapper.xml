<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="CategoryAuthe">
    <resultMap id="CategoryAutheMap" type="CategoryAuthe">
        <id column="id" property="id"/>
        <result column="shop_id" property="shopId"/>
        <result column="shop_name" property="shopName"/>
        <result column="discount_lower_limit" property="discountLowerLimit"/>
        <result column="category_auth_list" property="categoryAuthList"/>
        <result column="category_discount_list" property="categoryDiscountList"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        
    </resultMap>

    <sql id="tb">
        vega_category_authes
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        shop_id, shop_name, discount_lower_limit, category_auth_list, category_discount_list, created_at, updated_at
    </sql>

    <sql id="vals">
        #{shopId}, #{shopName}, #{discountLowerLimit}, #{categoryAuthList}, #{categoryDiscountList}, now(), now()
    </sql>

    <sql id="queryCondition">
        where 1 = 1
        
        <if test="shopId != null">and `shop_id` = #{shopId}</if>
        
        <if test="shopName != null">and `shop_name` = #{shopName}</if>

        <if test="createdAt != null">and `created_at` = #{createdAt}</if>
        
        <if test="updatedAt != null">and `updated_at` = #{updatedAt}</if>
        
        <if test="startAt != null">and created_at &gt; #{startAt}</if>
        <if test="endAt != null">and created_at &lt; #{endAt}</if>
    </sql>

    <sql id="order">
        order by id desc
    </sql>

    <insert id="create" parameterType="CategoryAuthe" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="findById" parameterType="long" resultMap="CategoryAutheMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

   <select id="findByIds" parameterType="list" resultMap="CategoryAutheMap">
        select id, <include refid="cols_exclude_id"/>
        from <include refid="tb"/>
        where id in
        <foreach collection="list" open="(" separator="," close=")"
                 item="item">
            #{item}
        </foreach>
        <include refid="order" />
    </select>

    <select id="findByShopId" parameterType="long" resultMap="CategoryAutheMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE `shop_id` = #{shopId}
    </select>

    <select id="findByShopIds" parameterType="list" resultMap="CategoryAutheMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        where shop_id in
        <foreach collection="list" open="(" separator="," close=")"
                 item="item">
            #{item}
        </foreach>
        <include refid="order" />
    </select>

    <update id="update" parameterType="CategoryAuthe">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="shopId != null">shop_id = #{shopId},</if>
            <if test="shopName != null">shop_name = #{shopName},</if>
            <if test="discountLowerLimit != null">discount_lower_limit = #{discountLowerLimit},</if>
            <if test="categoryAuthList != null">category_auth_list = #{categoryAuthList},</if>
            <if test="categoryDiscountList != null">category_discount_list = #{categoryDiscountList},</if>
            
            
            
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateDiscountLowerLimitByShopId" parameterType="map">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="discountLowerLimit != null">discount_lower_limit = #{discountLowerLimit},</if>

            updated_at=now()
        </set>
        WHERE shop_id = #{shopId}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <select id="paging" parameterType="list" resultMap="CategoryAutheMap">
        select id, <include refid="cols_exclude_id"/>
        from <include refid="tb"/>
        <include refid="queryCondition"/>
        order by id desc limit #{offset},#{limit}
    </select>

    <select id="count" parameterType="list" resultType="long">
        select count(1)
        from <include refid="tb"/>
        <include refid="queryCondition"/>
    </select>

</mapper>