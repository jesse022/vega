<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="FrontCategory">

    <select id="findCategoryByIdAndHasChildren" parameterType="long" resultMap="FrontCategoryMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id} and has_children=1 LIMIT 1
    </select>

    <select id="checkCategoryByIdAndLevelAndHasChildren" parameterType="map" resultType="long" >
        SELECT count(1)
        FROM <include refid="tb" />
        WHERE id = #{id}
        and level = #{level}
        and has_children = #{hasChildren}
        LIMIT 1
    </select>

    <update id="updatePidAndLevel" parameterType="FrontCategory">
        UPDATE <include refid="tb"/>
        <set>
            <if test="pid != null"> pid = #{pid}, </if>
            <if test="level != null"> level = #{level}, </if>
            updated_at=now()
        </set>
        WHERE id=#{id}
    </update>

</mapper>