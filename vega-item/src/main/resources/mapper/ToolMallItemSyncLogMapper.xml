<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2018 杭州端点网络科技有限公司, Code generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="ToolMallItemSyncLog">
    <resultMap id="ToolMallItemSyncLogMap" type="ToolMallItemSyncLog">
        <id column="id" property="id"/>
        <result column="type" property="type"/>
        <result column="status" property="status"/>
        <result column="begin_time" property="beginTime"/>
        <result column="end_time" property="endTime"/>
        <result column="page_num" property="pageNum"/>
        <result column="page_size" property="pageSize"/>
        <result column="sync_type" property="syncType"/>
        <result column="import_id" property="importId"/>
        <result column="item_import_json" property="itemImportJson"/>
        <result column="error_result" property="errorResult"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        
    </resultMap>

    <sql id="tb">
        vega_toolmall_item_sync_log
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        type, status, begin_time, end_time, page_num, page_size, sync_type, import_id, item_import_json, error_result, created_at, updated_at
    </sql>

    <sql id="vals">
        #{type}, #{status}, #{beginTime}, #{endTime}, #{pageNum}, #{pageSize}, #{syncType}, #{importId}, #{itemImportJson}, #{errorResult}, now(), now()
    </sql>

    <sql id="queryCondition">
        where 1 = 1
        
        <if test="type != null">and `type` = #{type}</if>
        
        <if test="status != null">and `status` = #{status}</if>

        <if test="importId != null">and `import_id` = #{importId}</if>
        
        <if test="startAt != null">and created_at &gt; #{startAt}</if>
        <if test="endAt != null">and created_at &lt; #{endAt}</if>
    </sql>

    <sql id="order">
        order by id desc
    </sql>

    <insert id="create" parameterType="ToolMallItemSyncLog" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="findById" parameterType="long" resultMap="ToolMallItemSyncLogMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

   <select id="findByIds" parameterType="list" resultMap="ToolMallItemSyncLogMap">
        select id, <include refid="cols_exclude_id"/>
        from <include refid="tb"/>
        where id in
        <foreach collection="list" open="(" separator="," close=")"
                 item="item">
            #{item}
        </foreach>
        <include refid="order" />
    </select>

    <update id="update" parameterType="ToolMallItemSyncLog">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="status != null">status = #{status},</if>
            <if test="itemUploadedJson != null">item_uploaded_json = #{itemUploadedJson},</if>
            <if test="importId != null">import_id = #{importId},</if>
            <if test="errorResult != null">error_result = #{errorResult},</if>
            
            
            
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <select id="paging" parameterType="list" resultMap="ToolMallItemSyncLogMap">
        select id, <include refid="cols_exclude_id"/>
        from <include refid="tb"/>
        <include refid="queryCondition"/>
        order by id desc limit #{offset},#{limit}
    </select>

    <select id="count" parameterType="list" resultType="long">
        select count(1)
        from <include refid="tb"/>
        <include refid="queryCondition"/>
    </select>

</mapper>