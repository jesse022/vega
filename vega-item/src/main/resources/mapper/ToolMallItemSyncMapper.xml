<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2018 杭州端点网络科技有限公司, Code generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="ToolMallItemSync">
    <resultMap id="ToolMallItemSyncMap" type="ToolMallItemSync">
        <id column="id" property="id"/>
        <result column="type" property="type"/>
        <result column="next_begin_time" property="nextBeginTime"/>
        <result column="extra_json" property="extraJson"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        
    </resultMap>

    <sql id="tb">
        vega_toolmall_item_sync
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        type, next_begin_time, extra_json, created_at, updated_at
    </sql>

    <sql id="vals">
        #{type}, #{nextBeginTime}, #{extraJson}, now(), now()
    </sql>

    <sql id="queryCondition">
        where 1 = 1
        
        <if test="type != null">and `type` = #{type}</if>
        
        <if test="startAt != null">and created_at &gt; #{startAt}</if>
        <if test="endAt != null">and created_at &lt; #{endAt}</if>
    </sql>

    <sql id="order">
        order by id desc
    </sql>

    <insert id="create" parameterType="ToolMallItemSync" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="findById" parameterType="long" resultMap="ToolMallItemSyncMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

   <select id="findByIds" parameterType="list" resultMap="ToolMallItemSyncMap">
        select id, <include refid="cols_exclude_id"/>
        from <include refid="tb"/>
        where id in
        <foreach collection="list" open="(" separator="," close=")"
                 item="item">
            #{item}
        </foreach>
        <include refid="order" />
    </select>

    <update id="update" parameterType="ToolMallItemSync">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="nextBeginTime != null">next_begin_time = #{nextBeginTime},</if>
            <if test="extraJson != null">extra_json = #{extraJson},</if>

            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateByType" parameterType="ToolMallItemSync">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="nextBeginTime != null">next_begin_time = #{nextBeginTime},</if>
            <if test="extraJson != null">extra_json = #{extraJson},</if>

            updated_at=now()
        </set>
        WHERE type = #{type}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <select id="paging" parameterType="list" resultMap="ToolMallItemSyncMap">
        select id, <include refid="cols_exclude_id"/>
        from <include refid="tb"/>
        <include refid="queryCondition"/>
        order by id desc limit #{offset},#{limit}
    </select>

    <select id="count" parameterType="list" resultType="long">
        select count(1)
        from <include refid="tb"/>
        <include refid="queryCondition"/>
    </select>

</mapper>