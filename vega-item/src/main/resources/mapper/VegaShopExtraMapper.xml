<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="VegaShopExtra">
    <resultMap id="VegaShopExtraMap" type="VegaShopExtra">
        <id column="id" property="id"/>
        <result column="shop_id" property="shopId"/>
        <result column="shop_pid" property="shopPid"/>
        <result column="shop_parent_name" property="shopParentName"/>
        <result column="shop_name" property="shopName"/>
        <result column="shop_status" property="shopStatus"/>
        <result column="shop_type" property="shopType"/>
        <result column="shop_authorize" property="shopAuthorize"/>
        <result column="is_old_member" property="isOldMember"/>
        <result column="user_id" property="userId"/>
        <result column="user_name" property="userName"/>
        <result column="purchase_discount" property="purchaseDiscount"/>
        <result column="discount_lower_limit" property="discountLowerLimit"/>
        <result column="member_discount_json" property="memberDiscountJson"/>
        <result column="total_credit" property="totalCredit"/>
        <result column="available_credit" property="availableCredit"/>
        <result column="credit_payment_days" property="creditPaymentDays"/>
        <result column="is_credit_available" property="isCreditAvailable"/>
        <result column="bank_account" property="bankAccount"/>
        <result column="contact_name" property="contactName"/>
        <result column="contact_phone" property="contactPhone"/>
        <result column="province_id" property="provinceId"/>
        <result column="province" property="province"/>
        <result column="city_id" property="cityId"/>
        <result column="city" property="city"/>
        <result column="region_id" property="regionId"/>
        <result column="region" property="region"/>
        <result column="street" property="street"/>
        <result column="postcode" property="postcode"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        
    </resultMap>

    <sql id="tb">
        vega_shop_extras
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        shop_id, shop_pid, shop_parent_name, shop_name, shop_status, shop_type, shop_authorize, is_old_member, user_id, user_name,
        purchase_discount, discount_lower_limit, member_discount_json, total_credit, available_credit, credit_payment_days,
        is_credit_available, bank_account, contact_name, contact_phone,
        province_id, province, city_id, city, region_id, region, street, postcode,
        created_at, updated_at
    </sql>

    <sql id="vals">
        #{shopId}, #{shopPid}, #{shopParentName}, #{shopName}, #{shopStatus}, #{shopType}, #{shopAuthorize}, #{isOldMember}, #{userId}, #{userName},
        #{purchaseDiscount}, #{discountLowerLimit}, #{memberDiscountJson}, #{totalCredit}, #{availableCredit}, #{creditPaymentDays},
        #{isCreditAvailable}, #{bankAccount}, #{contactName}, #{contactPhone},
        #{provinceId}, #{province}, #{cityId}, #{city}, #{regionId}, #{region}, #{street}, #{postcode},
        now(), now()
    </sql>

    <sql id="queryCondition">
        where 1 = 1

        <if test="shopId != null">and `shop_id` = #{shopId}</if>
        
        <if test="shopPid != null">and `shop_pid` = #{shopPid}</if>
        
        <if test="shopType != null">and `shop_type` = #{shopType}</if>

        <if test="shopAuthorize != null">and `shop_authorize` = #{shopAuthorize}</if>

        <if test="isOldMember != null">and `is_old_member` = #{isOldMember}</if>

        <if test="shopName != null">and `shop_name` LIKE CONCAT ('%',#{shopName},'%')</if>

        <if test="userName != null">and `user_name` LIKE CONCAT ('%',#{userName},'%')</if>

        <if test="shopStatus != null">and `shop_status` = #{shopStatus}</if>

        <if test="userId != null">and `user_id` = #{userId}</if>
        
        <if test="purchaseDiscount != null">and `purchase_discount` = #{purchaseDiscount}</if>
        
        <if test="memberDiscountJson != null">and `member_discount_json` = #{memberDiscountJson}</if>
        
        <if test="credit != null">and `credit` = #{credit}</if>
        
        <if test="createdAt != null">and `created_at` = #{createdAt}</if>
        
        <if test="updatedAt != null">and `updated_at` = #{updatedAt}</if>
        
        <if test="startAt != null">and created_at &gt; #{startAt}</if>
        <if test="endAt != null">and created_at &lt; #{endAt}</if>
    </sql>

    <sql id="order">
        order by id desc
    </sql>

    <insert id="create" parameterType="VegaShopExtra" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="findById" parameterType="long" resultMap="VegaShopExtraMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

   <select id="findByIds" parameterType="list" resultMap="VegaShopExtraMap">
        select id, <include refid="cols_exclude_id"/>
        from <include refid="tb"/>
        where id in
        <foreach collection="list" open="(" separator="," close=")"
                 item="item">
            #{item}
        </foreach>
        <include refid="order" />
    </select>

    <update id="update" parameterType="VegaShopExtra">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="shopId != null">shop_id = #{shopId},</if>
            <if test="shopPid != null">shop_pid = #{shopPid},</if>
            <if test="shopParentName != null">shop_parent_name = #{shopParentName},</if>
            <if test="shopName != null">shop_name = #{shopName},</if>
            <if test="shopStatus != null">shop_status = #{shopStatus},</if>
            <if test="shopType != null">shop_type = #{shopType},</if>
            <if test="shopAuthorize != null">shop_authorize = #{shopAuthorize},</if>
            <if test="isOldMember != null">is_old_member = #{isOldMember},</if>
            <if test="purchaseDiscount != null">purchase_discount = #{purchaseDiscount},</if>
            <if test="discountLowerLimit != null">discount_lower_limit = #{discountLowerLimit},</if>
            <if test="memberDiscountJson != null">member_discount_json = #{memberDiscountJson},</if>
            <if test="totalCredit != null">total_credit = #{totalCredit},</if>
            <if test="availableCredit != null">available_credit = #{availableCredit},</if>
            <if test="creditPaymentDays != null">credit_payment_days = #{creditPaymentDays},</if>
            <if test="isCreditAvailable != null">is_credit_available = #{isCreditAvailable},</if>
            <if test="bankAccount != null">bank_account = #{bankAccount},</if>
            <if test="contactName != null">contact_name = #{contactName},</if>
            <if test="contactPhone != null">contact_phone = #{contactPhone},</if>
            <if test="provinceId != null">province_id = #{provinceId},</if>
            <if test="province != null">province = #{province},</if>
            <if test="cityId != null">city_id = #{cityId},</if>
            <if test="city != null">city = #{city},</if>
            <if test="regionId != null">region_id = #{regionId},</if>
            <if test="region != null">region = #{region},</if>
            <if test="street != null">street = #{street},</if>
            <if test="postcode != null">postcode = #{postcode},</if>
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateByShopId" parameterType="VegaShopExtra">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="shopPid != null">shop_pid = #{shopPid},</if>
            <if test="shopParentName != null">shop_parent_name = #{shopParentName},</if>
            <if test="shopName != null">shop_name = #{shopName},</if>
            <if test="shopStatus != null">shop_status = #{shopStatus},</if>
            <if test="shopType != null">shop_type = #{shopType},</if>
            <if test="shopAuthorize != null">shop_authorize = #{shopAuthorize},</if>
            <if test="isOldMember != null">is_old_member = #{isOldMember},</if>
            <if test="purchaseDiscount != null">purchase_discount = #{purchaseDiscount},</if>
            <if test="discountLowerLimit != null">discount_lower_limit = #{discountLowerLimit},</if>
            <if test="memberDiscountJson != null">member_discount_json = #{memberDiscountJson},</if>
            <if test="totalCredit != null">total_credit = #{totalCredit},</if>
            <if test="availableCredit != null">available_credit = #{availableCredit},</if>
            <if test="creditPaymentDays != null">credit_payment_days = #{creditPaymentDays},</if>
            <if test="isCreditAvailable != null">is_credit_available = #{isCreditAvailable},</if>
            <if test="bankAccount != null">bank_account = #{bankAccount},</if>
            <if test="contactName != null">contact_name = #{contactName},</if>
            <if test="contactPhone != null">contact_phone = #{contactPhone},</if>
            <if test="provinceId != null">province_id = #{provinceId},</if>
            <if test="province != null">province = #{province},</if>
            <if test="cityId != null">city_id = #{cityId},</if>
            <if test="city != null">city = #{city},</if>
            <if test="regionId != null">region_id = #{regionId},</if>
            <if test="region != null">region = #{region},</if>
            <if test="street != null">street = #{street},</if>
            <if test="postcode != null">postcode = #{postcode},</if>
            updated_at=now()
        </set>
        WHERE shop_id = #{shopId}
    </update>

    <update id="updateStatusByShopId" parameterType="VegaShopExtra">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="shopPid != -1">shop_pid = #{shopPid},</if>
            <if test="shopParentName != ''">shop_parent_name = #{shopParentName},</if>
            <if test="shopStatus != null">shop_status = #{shopStatus},</if>
            <if test="shopType != null">shop_type = #{shopType},</if>
            updated_at=now()
        </set>
        WHERE shop_id = #{shopId}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <select id="paging" parameterType="list" resultMap="VegaShopExtraMap">
        select id, <include refid="cols_exclude_id"/>
        from <include refid="tb"/>
        <include refid="queryCondition"/>
        order by id desc limit #{offset},#{limit}
    </select>

    <select id="count" parameterType="list" resultType="long">
        select count(1)
        from <include refid="tb"/>
        <include refid="queryCondition"/>
    </select>

    <select id="countNew" parameterType="map" resultType="long">
        select count(1)
        from <include refid="tb"/>
        <include refid="queryCondition"/>
    </select>

    <select id="findByShopId" parameterType="long" resultMap="VegaShopExtraMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE shop_id = #{shopId}
    </select>

    <select id="findByShopIds" parameterType="list" resultMap="VegaShopExtraMap">
        SELECT
        <include refid="cols_all"/>
        FROM
        <include refid="tb"/>
        WHERE shop_id IN
        <foreach item="shopId" collection="list" open="(" separator="," close=")">
            #{shopId}
        </foreach>
    </select>
    <!--根据shopIds获取一级经销商数据-->
    <select id="findFirstDealerByShopIds" parameterType="list" resultMap="VegaShopExtraMap">
        SELECT
        <include refid="cols_all"/>
        FROM
        <include refid="tb"/>
        WHERE shop_id IN
        <foreach item="shopId" collection="list" open="(" separator="," close=")">
            #{shopId}
        </foreach>
        and shop_type=2
    </select>

    <select id="findByUserId" parameterType="long" resultMap="VegaShopExtraMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE user_id = #{userId}
    </select>

    <select id="listAllShopByPid" parameterType="long" resultMap="VegaShopExtraMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE shop_pid = #{shopPid}
    </select>

    <select id="listShopByNameAndType" parameterType="map" resultMap="VegaShopExtraMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE `shop_type` = #{type}
        and shop_name LIKE CONCAT ('%',#{name},'%')
        and shop_status = 1
    </select>

    <select id="listShopByPidAndName" parameterType="map" resultMap="VegaShopExtraMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE `shop_pid` = #{pid} and shop_name LIKE CONCAT ('%',#{name},'%')
              and shop_status = 1
    </select>

    <update id="updateStatus" parameterType="map">
        UPDATE
        <include refid="tb"/>
        <set>
            shop_status = #{shopStatus},
            updated_at=now()
        </set>
        WHERE shop_id = #{shopId}
    </update>

    <update id="changeCreditStatusByShopId" parameterType="map">
        UPDATE
        <include refid="tb"/>
        <set>
            is_credit_available = #{isAvailable},
            updated_at=now()
        </set>
        WHERE shop_id = #{shopId}
    </update>

    <update id="changeDiscountLowerLimit" parameterType="map">
        UPDATE
        <include refid="tb"/>
        <set>
            discount_lower_limit = #{discountLowerLimit},
            updated_at=now()
        </set>
        WHERE shop_id = #{shopId}
    </update>

    <update id="batchFrozeShopCredit" parameterType="list">
        UPDATE
        <include refid="tb"/>
        <set>
            is_credit_available = false ,
            updated_at=now()
        </set>
        where shop_id in
        <foreach collection="list" open="(" separator="," close=")" item="item">
            #{item}
        </foreach>
    </update>

    <select id="listSupplierIds" resultType="long">
        SELECT shop_id
        FROM <include refid="tb" />
        WHERE shop_type = 1
    </select>

    <select id="pagingIds" parameterType="map" resultType="long">
        select shop_id
        from <include refid="tb"/>
        order by id desc limit #{offset}, #{limit}
    </select>

    <select id="suggestionByName" parameterType="string" resultMap="VegaShopExtraMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE shop_name LIKE CONCAT ('%',#{name},'%') and shop_status = 1
    </select>

</mapper>
