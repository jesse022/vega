<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2018 杭州端点网络科技有限公司, Code generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="VegaBrandExtra">
    <resultMap id="VegaBrandExtraMap" type="VegaBrandExtra">
        <id column="id" property="id"/>
        <result column="brand_id" property="brandId"/>
        <result column="detail" property="detail"/>
        <result column="extra_json" property="extraJson"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        
    </resultMap>

    <sql id="tb">
        vega_brands_extras
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        brand_id, detail, extra_json, created_at, updated_at
    </sql>

    <sql id="vals">
        #{brandId}, #{detail}, #{extraJson}, now(), now()
    </sql>

    <sql id="queryCondition">
        where 1 = 1

        <if test="brandId != null">and `brand_id` = #{brandId}</if>
        
        <if test="detail != null">and `detail` = #{detail}</if>
        
        <if test="extraJson != null">and `extra_json` = #{extraJson}</if>
        
        <if test="startAt != null">and created_at &gt; #{startAt}</if>
        <if test="endAt != null">and created_at &lt; #{endAt}</if>
    </sql>

    <sql id="order">
        order by id desc
    </sql>

    <insert id="create" parameterType="VegaBrandExtra" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="findById" parameterType="long" resultMap="VegaBrandExtraMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

   <select id="findByIds" parameterType="list" resultMap="VegaBrandExtraMap">
        select id, <include refid="cols_exclude_id"/>
        from <include refid="tb"/>
        where id in
        <foreach collection="list" open="(" separator="," close=")"
                 item="item">
            #{item}
        </foreach>
        <include refid="order" />
    </select>

    <select id="findByBrandId" parameterType="long" resultMap="VegaBrandExtraMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE brand_id = #{brandId}
    </select>

    <update id="update" parameterType="VegaBrandExtra">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="brandId != null">`brand_id` = #{brandId},</if>
            <if test="detail != null">`detail` = #{detail},</if>
            <if test="extraJson != null">`extra_json` = #{extraJson},</if>
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateByBrandId" parameterType="VegaBrandExtra">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="detail != null">`detail` = #{detail},</if>
            <if test="extraJson != null">`extra_json` = #{extraJson},</if>
            updated_at=now()
        </set>
        WHERE brand_id = #{brandId}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>

</mapper>
