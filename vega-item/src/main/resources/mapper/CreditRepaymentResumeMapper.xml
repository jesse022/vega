<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="CreditRepaymentResume">
    <resultMap id="CreditRepaymentResumeMap" type="CreditRepaymentResume">
        <id column="id" property="id"/>
        <result column="alter_resume_id" property="alterResumeId"/>
        <result column="fee" property="fee"/>
        <result column="last_debt_amount" property="lastDebtAmount"/>
        <result column="remain_amount" property="remainAmount"/>
        <result column="fine_amount" property="fineAmount"/>
        <result column="beyond_amount" property="beyondAmount"/>
        <result column="type" property="type"/>
        <result column="status" property="status"/>
        <result column="extra_json" property="extraJson"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="tb">
        vega_credit_repayment_resumes
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        alter_resume_id, fee, last_debt_amount, remain_amount, fine_amount, beyond_amount,
        `type`, status, extra_json, created_at, updated_at
    </sql>

    <sql id="vals">
        #{alterResumeId}, #{fee}, #{lastDebtAmount}, #{remainAmount}, #{fineAmount}, #{beyondAmount},
         #{type}, #{status}, #{extraJson}, now(), now()
    </sql>

    <sql id="queryCondition">
        where 1 = 1
        
        <if test="alterResumeId != null">and `alter_resume_id` = #{alterResumeId}</if>
        
        <if test="fee != null">and `fee` = #{fee}</if>

        <if test="type != null">and `type` = #{type}</if>

        <if test="status != null">and `status` = #{status}</if>
        
        <if test="extraJson != null">and `extra_json` = #{extraJson}</if>
        
        <if test="createdAt != null">and `created_at` = #{createdAt}</if>
        
        <if test="updatedAt != null">and `updated_at` = #{updatedAt}</if>
        
        <if test="startAt != null">and created_at &gt; #{startAt}</if>
        <if test="endAt != null">and created_at &lt; #{endAt}</if>
    </sql>

    <sql id="order">
        order by id desc
    </sql>

    <insert id="create" parameterType="CreditRepaymentResume" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="findById" parameterType="long" resultMap="CreditRepaymentResumeMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

   <select id="findByIds" parameterType="list" resultMap="CreditRepaymentResumeMap">
        select id, <include refid="cols_exclude_id"/>
        from <include refid="tb"/>
        where id in
        <foreach collection="list" open="(" separator="," close=")"
                 item="item">
            #{item}
        </foreach>
        <include refid="order" />
    </select>

    <update id="update" parameterType="CreditRepaymentResume">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="alterResumeId != null">alter_resume_id = #{alterResumeId},</if>
            <if test="fee != null">fee = #{fee},</if>
            <if test="status != null">status = #{status},</if>
            <if test="extraJson != null">extra_json = #{extraJson},</if>
            
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <select id="paging" parameterType="list" resultMap="CreditRepaymentResumeMap">
        select id, <include refid="cols_exclude_id"/>
        from <include refid="tb"/>
        <include refid="queryCondition"/>
        order by id desc limit #{offset},#{limit}
    </select>

    <select id="count" parameterType="list" resultType="long">
        select count(1)
        from <include refid="tb"/>
        <include refid="queryCondition"/>
    </select>

    <select id="findByAlterResumeId" parameterType="long" resultMap="CreditRepaymentResumeMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE alter_resume_id = #{alterResumeId}
        order by id DESC
    </select>

    <select id="findLastRepaymentByAlterResumeId" parameterType="long" resultMap="CreditRepaymentResumeMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE alter_resume_id = #{alterResumeId}
        order by id DESC limit 1
    </select>

</mapper>