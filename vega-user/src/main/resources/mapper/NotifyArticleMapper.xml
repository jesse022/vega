<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="NotifyArticle">
    <resultMap id="NotifyArticleMap" type="NotifyArticle">
        <id column="id" property="id"/>
        <result column="theme" property="theme"/>
        <result column="content" property="content"/>
        <result column="status" property="status"/>
        <result column="file_url" property="fileUrl"/>
        <result column="notify_supplier" property="notifySupplier"/>
        <result column="notify_dealer_first" property="notifyDealerFirst"/>
        <result column="notify_dealer_second" property="notifyDealerSecond"/>
        <result column="extra_json" property="extraJson"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        
    </resultMap>

    <sql id="tb">
        vega_notify_articles
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        theme, content, status, file_url, notify_supplier, notify_dealer_first, notify_dealer_second, extra_json, created_at, updated_at
    </sql>

    <sql id="vals">
        #{theme}, #{content}, #{status}, #{fileUrl}, #{notifySupplier}, #{notifyDealerFirst}, #{notifyDealerSecond}, #{extraJson}, now(), now()
    </sql>

    <sql id="queryCondition">
        where 1 = 1
        
        <if test="theme != null">and `theme` = #{theme}</if>
        
        <if test="content != null">and `content` = #{content}</if>
        
        <if test="status != null">and `status` = #{status}</if>
        
        <if test="fileUrl != null">and `file_url` = #{fileUrl}</if>
        
        <if test="notifySupplier != null">and `notify_supplier` = #{notifySupplier}</if>
        
        <if test="notifyDealerFirst != null">and `notify_dealer_first` = #{notifyDealerFirst}</if>
        
        <if test="notifyDealerSecond != null">and `notify_dealer_second` = #{notifyDealerSecond}</if>
        
        <if test="extraJson != null">and `extra_json` = #{extraJson}</if>
        
        <if test="createdAt != null">and `created_at` = #{createdAt}</if>
        
        <if test="updatedAt != null">and `updated_at` = #{updatedAt}</if>
        
        <if test="startAt != null">and created_at &gt; #{startAt}</if>
        <if test="endAt != null">and created_at &lt; #{endAt}</if>
    </sql>

    <sql id="order">
        order by id desc
    </sql>

    <insert id="create" parameterType="NotifyArticle" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="findById" parameterType="long" resultMap="NotifyArticleMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

   <select id="findByIds" parameterType="list" resultMap="NotifyArticleMap">
        select id, <include refid="cols_exclude_id"/>
        from <include refid="tb"/>
        where id in
        <foreach collection="list" open="(" separator="," close=")"
                 item="item">
            #{item}
        </foreach>
        <include refid="order" />
    </select>

    <update id="update" parameterType="NotifyArticle">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="theme != null">theme = #{theme},</if>
            <if test="content != null">content = #{content},</if>
            <if test="status != null">status = #{status},</if>
            <if test="fileUrl != null">file_url = #{fileUrl},</if>
            <if test="notifySupplier != null">notify_supplier = #{notifySupplier},</if>
            <if test="notifyDealerFirst != null">notify_dealer_first = #{notifyDealerFirst},</if>
            <if test="notifyDealerSecond != null">notify_dealer_second = #{notifyDealerSecond},</if>
            <if test="extraJson != null">extra_json = #{extraJson},</if>
            
            
            
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <select id="paging" parameterType="list" resultMap="NotifyArticleMap">
        select id, <include refid="cols_exclude_id"/>
        from <include refid="tb"/>
        <include refid="queryCondition"/>
        order by id desc limit #{offset},#{limit}
    </select>

    <select id="count" parameterType="list" resultType="long">
        select count(1)
        from <include refid="tb"/>
        <include refid="queryCondition"/>
    </select>

</mapper>