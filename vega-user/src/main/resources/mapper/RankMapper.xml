<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="Rank">
    <resultMap id="RankMap" type="Rank">
        <id column="id" property="id"/>
        <id column="pid" property="pid"/>
        <result column="name" property="name"/>
        <result column="growth_value_start" property="growthValueStart"/>
        <result column="growth_value_end" property="growthValueEnd"/>
        <result column="discount" property="discount"/>
        <result column="extra_json" property="extraJson"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="tb">
        vega_ranks
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        pid,name, growth_value_start, growth_value_end, discount, extra_json, created_at, updated_at
    </sql>

    <sql id="vals">
        #{pid},#{name}, #{growthValueStart}, #{growthValueEnd}, #{discount}, #{extraJson}, now(), now()
    </sql>

    <sql id="queryCondition">
        where 1 = 1
        
        <if test="name != null">and `name` = #{name}</if>
        
        <if test="growthValueStart != null">and `growth_value_start` = #{growthValueStart}</if>
        
        <if test="growthValueEnd != null">and `growth_value_end` = #{growthValueEnd}</if>
        
        <if test="discount != null">and `discount` = #{discount}</if>
        
        <if test="extraJson != null">and `extra_json` = #{extraJson}</if>

        
        <if test="startAt != null">and created_at &gt; #{startAt}</if>
        <if test="endAt != null">and created_at &lt; #{endAt}</if>
    </sql>

    <sql id="order">
        order by id desc
    </sql>

    <insert id="create" parameterType="Rank" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="findById" parameterType="long" resultMap="RankMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>



    <select id="findBaseRank" resultMap="RankMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE pid = 0
    </select>

    <select id="findAll"  resultMap="RankMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
    </select>

    <select id="findByPid" parameterType="long" resultMap="RankMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE pid = #{pid}
    </select>

    <select id="findByIntegral" parameterType="long" resultMap="RankMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE  #{integral} BETWEEN growth_value_start AND growth_value_end
    </select>




   <select id="findByIds" parameterType="list" resultMap="RankMap">
        select id, <include refid="cols_exclude_id"/>
        from <include refid="tb"/>
        where id in
        <foreach collection="list" open="(" separator="," close=")"
                 item="item">
            #{item}
        </foreach>
        <include refid="order" />
    </select>

    <update id="update" parameterType="Rank">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="growthValueStart != null">growth_value_start = #{growthValueStart},</if>
            <if test="growthValueEnd != null">growth_value_end = #{growthValueEnd},</if>
            <if test="discount != null">discount = #{discount},</if>
            <if test="extraJson != null">extra_json = #{extraJson},</if>
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <select id="paging" parameterType="list" resultMap="RankMap">
        select id, <include refid="cols_exclude_id"/>
        from <include refid="tb"/>
        <include refid="queryCondition"/>
        limit #{offset},#{limit}
    </select>

    <select id="count" parameterType="list" resultType="long">
        select count(1)
        from <include refid="tb"/>
        <include refid="queryCondition"/>
    </select>



    <update id="updateRank" parameterType="map">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="growthValueStart != null">growth_value_start = #{growthValueStart},</if>
            <if test="growthValueEnd != null">growth_value_end = #{growthValueEnd},</if>
            <if test="discount != null">discount = #{discount},</if>
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>





</mapper>