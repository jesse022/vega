<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="DeliveryScope">
    <resultMap id="DeliveryScopeMap" type="DeliveryScope">
        <id column="id" property="id"/>
        <result column="shop_id" property="shopId"/>
        <result column="shop_name" property="shopName"/>
        <result column="pId" property="pId"/>
        <result column="scope" property="scope"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>

    </resultMap>

    <sql id="tb">
        vega_delivery_scopes
    </sql>

    <sql id="cols_all">
        id,
        <include refid="cols_exclude_id"/>
    </sql>

    <sql id="cols_exclude_id">
        shop_id, shop_name, `pId`, scope, created_at, updated_at
    </sql>

    <sql id="vals">
        #{shopId}, #{shopName}, #{pId}, #{scope}, now(), now()
    </sql>

    <sql id="queryCondition">
        where 1 = 1

        <if test="e
         != null">and `shop_id` = #{shopId}
        </if>

        <if test="shopName != null">and `shop_name` = #{shopName}</if>

        <if test="pId != null">and `pId` = #{pId}</if>

        <if test="createdAt != null">and `created_at` = #{createdAt}</if>

        <if test="updatedAt != null">and `updated_at` = #{updatedAt}</if>

        <if test="startAt != null">and created_at &gt; #{startAt}</if>
        <if test="endAt != null">and created_at &lt; #{endAt}</if>
    </sql>

    <sql id="order">
        order by id desc
    </sql>

    <insert id="create" parameterType="DeliveryScope" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb"/>
        (<include refid="cols_exclude_id"/>)
        VALUES
        (<include refid="vals"/>)
    </insert>

    <select id="findById" parameterType="long" resultMap="DeliveryScopeMap">
        SELECT
        <include refid="cols_all"/>
        FROM
        <include refid="tb"/>
        WHERE id = #{id}
    </select>

    <select id="findDeliveryScopeByShopId" parameterType="long" resultMap="DeliveryScopeMap">
        SELECT
        <include refid="cols_all"/>
        FROM
        <include refid="tb"/>
        WHERE shop_id = #{shopId}
    </select>

    <select id="findDeliveryScopeByPId" parameterType="long" resultMap="DeliveryScopeMap">
        SELECT
        <include refid="cols_all"/>
        FROM
        <include refid="tb"/>
        WHERE pId = #{pId}
    </select>

    <select id="findAllShopIds" resultType="long">
        SELECT  `shop_id`
        FROM
        <include refid="tb"/>
    </select>

    <select id="findByIds" parameterType="list" resultMap="DeliveryScopeMap">
        select id,
        <include refid="cols_exclude_id"/>
        from
        <include refid="tb"/>
        where id in
        <foreach collection="list" open="(" separator="," close=")"
                 item="item">
            #{item}
        </foreach>
        <include refid="order"/>
    </select>

    <update id="update" parameterType="DeliveryScope">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="shopId != null">shop_id = #{shopId},</if>
            <if test="shopName != null">shop_name = #{shopName},</if>
            <if test="pId != null">pId = #{pId},</if>
            <if test="scope != null">scope = #{scope},</if>


            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateScopeByShopId" parameterType="DeliveryScope">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="scope != null">scope = #{scope},</if>

            updated_at=now()
        </set>
        WHERE shop_id = #{shopId}
    </update>

    <update id="updateScope" parameterType="DeliveryScope">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="scope != null">scope = #{scope},</if>

            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM
        <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <select id="paging" parameterType="list" resultMap="DeliveryScopeMap">
        select id,
        <include refid="cols_exclude_id"/>
        from
        <include refid="tb"/>
        <include refid="queryCondition"/>
        order by id desc limit #{offset},#{limit}
    </select>

    <select id="count" parameterType="list" resultType="long">
        select count(1)
        from
        <include refid="tb"/>
        <include refid="queryCondition"/>
    </select>

</mapper>