<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2017 杭州端点网络科技有限公司, Code generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="ShopUserExtras">
    <resultMap id="ShopUserExtrasMap" type="ShopUserExtras">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="mobile" property="mobile"/>
        <result column="user_name" property="userName"/>
        <result column="shop_id" property="shopId"/>
        <result column="shop_name" property="shopName"/>
        <result column="user_type" property="userType"/>
        <result column="user_status" property="userStatus"/>
        <result column="company_name" property="companyName"/>
        <result column="contact_name" property="contactName"/>
        <result column="contact_phone" property="contactPhone"/>
        <result column="contact_email" property="contactEmail"/>
        <result column="province_id" property="provinceId"/>
        <result column="province" property="province"/>
        <result column="city_id" property="cityId"/>
        <result column="city" property="city"/>
        <result column="region_id" property="regionId"/>
        <result column="region" property="region"/>
        <result column="street" property="street"/>
        <result column="postcode" property="postcode"/>
        <result column="extra_json" property="extraJson"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>


    <sql id="tb">
        vega_shop_users_extras
    </sql>

    <sql id="cols_all">
        id,
        <include refid="cols_exclude_id"/>
    </sql>

    <sql id="cols_exclude_id">
        user_id, mobile, user_name, shop_id, shop_name, user_type, user_status, company_name, contact_name, contact_phone, contact_email, province_id,
        province, city_id, city, region_id, region, street, postcode, extra_json, created_at, updated_at
    </sql>

    <sql id="vals">
        #{userId}, #{mobile}, #{userName}, #{shopId}, #{shopName},#{userType}, #{userStatus}, #{companyName}, #{contactName}, #{contactPhone}, #{contactEmail}, #{provinceId},
        #{province}, #{cityId}, #{city}, #{regionId}, #{region}, #{street}, #{postcode}, #{extraJson}, now(), now()
    </sql>

    <sql id="queryCondition">
        where 1 = 1

        <if test="userId != null">and `user_id` = #{userId}</if>
        <if test="mobile != null">and `mobile` = #{mobile}</if>
        <if test="userName != null">and `user_name` = #{userName}</if>
        <if test="shopId != null">and `shop_id` = #{shopId}</if>
        <if test="shopName != null">and `shop_name` = #{shopName}</if>
        <if test="companyName != null">and `company_name` = #{companyName}</if>
        <if test="contactName != null">and `contact_name` = #{contactName}</if>
        <if test="contactEmail != null">and `contact_email` = #{contactEmail}</if>
        <if test="extraJson != null">and `extra_json` = #{extraJson}</if>
        <if test="startAt != null">and created_at &gt; #{startAt}</if>
        <if test="endAt != null">and created_at &lt; #{endAt}</if>
    </sql>


    <sql id="order">
        order by id desc
    </sql>

    <sql id="custom_sort">
        <if test="sortBy != null">
            <if test="sortBy == 'id'">ORDER BY id
                <include refid="custom_sort_type"/>
            </if>
            <if test="sortBy == 'updatedAt'">ORDER BY updated_at
                <include refid="custom_sort_type"/>
            </if>
        </if>
    </sql>

    <sql id="custom_sort_type">
        <if test="sortType != null">
            <if test="sortType == 1">ASC</if>
            <if test="sortType == 2">DESC</if>
        </if>
    </sql>

    <insert id="create" parameterType="ShopUserExtras" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb"/>
        (<include refid="cols_exclude_id"/>)
        VALUES
        (<include refid="vals"/>)
    </insert>

    <select id="findById" parameterType="long" resultMap="ShopUserExtrasMap">
        SELECT
        <include refid="cols_all"/>
        FROM
        <include refid="tb"/>
        WHERE id = #{id}
    </select>

    <select id="findByMobile" parameterType="string" resultMap="ShopUserExtrasMap">
        SELECT
        <include refid="cols_all"/>
        FROM
        <include refid="tb"/>
        WHERE mobile = #{mobile}
    </select>


    <select id="findByMobileAndShopId" parameterType="map" resultMap="ShopUserExtrasMap">
        SELECT
        <include refid="cols_all"/>
        FROM
        <include refid="tb"/>
        WHERE mobile = #{mobile} AND shop_id=#{shopId}
    </select>

    <select id="findByUserId" parameterType="long" resultMap="ShopUserExtrasMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE user_id = #{userId}
    </select>

    <select id="findByIds" parameterType="list" resultMap="ShopUserExtrasMap">
        select id,
        <include refid="cols_exclude_id"/>
        from
        <include refid="tb"/>
        where id in
        <foreach collection="list" open="(" separator="," close=")"
                 item="item">
            #{item}
        </foreach>
        <include refid="order"/>
    </select>

    <update id="update" parameterType="ShopUserExtras">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="mobile != null">mobile = #{mobile},</if>
            <if test="userName != null">user_name = #{userName},</if>
            <if test="shopId != null">shop_id = #{shopId},</if>
            <if test="shopName != null">shop_name = #{shopName},</if>
            <if test="userType != null">user_type = #{userType},</if>
            <if test="userStatus != null">user_status = #{userStatus},</if>
            <if test="companyName != null">company_name = #{companyName},</if>
            <if test="contactName != null">contact_name = #{contactName},</if>
            <if test="contactPhone != null">contact_phone = #{contactPhone},</if>
            <if test="contactEmail != null">contact_email = #{contactEmail},</if>
            <if test="provinceId != null">province_id = #{provinceId},</if>
            <if test="province != null">province = #{province},</if>
            <if test="cityId != null">city_id = #{cityId},</if>
            <if test="city != null">city = #{city},</if>
            <if test="regionId != null">region_id = #{regionId},</if>
            <if test="region != null">region = #{region},</if>
            <if test="street != null">street = #{street},</if>
            <if test="postcode != null">postcode = #{postcode},</if>
            <if test="extraJson != null">extra_json = #{extraJson},</if>


            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <update id="refreshShopUserByUserId" parameterType="map">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="userName != null">user_name = #{userName},</if>
            <if test="mobile != null">mobile = #{mobile},</if>
            <if test="userType != null">user_type = #{userType},</if>
            <if test="userStatus != null">user_status = #{userStatus},</if>
            updated_at=now()
        </set>
        WHERE user_id = #{userId}

    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM
        <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <delete id="deleteByUserId" parameterType="long">
        DELETE FROM
        <include refid="tb"/>
        WHERE user_id = #{userId}
    </delete>

    <delete id="deleteByShopId" parameterType="long">
        DELETE FROM
        <include refid="tb"/>
        WHERE shop_id = #{shopId}
    </delete>

    <select id="paging" parameterType="list" resultMap="ShopUserExtrasMap">
        select id,
        <include refid="cols_exclude_id"/>
        from
        <include refid="tb"/>
        <include refid="queryCondition"/>
        order by id desc limit #{offset},#{limit}
    </select>

    <select id="count" parameterType="list" resultType="long">
        select count(1)
        from
        <include refid="tb"/>
        <include refid="queryCondition"/>
    </select>

</mapper>