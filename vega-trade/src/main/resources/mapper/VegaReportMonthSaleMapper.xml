<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2018 杭州端点网络科技有限公司, Code generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="VegaReportMonthSale">

    <resultMap id="VegaReportMonthSaleMap" type="VegaReportMonthSale">
        <id column="id" property="id"/>
        <result column="year" property="year"/>
        <result column="month" property="month"/>
        <result column="status" property="status"/>
        <result column="order_count" property="orderCount"/>
        <result column="order_fee" property="orderFee"/>
        <result column="order_member" property="orderMember"/>
        <result column="visitor_json" property="visitorJson"/>
        <result column="category_json" property="categoryJson"/>
        <result column="extra_json" property="extraJson"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="tb">
        vega_report_month_sale
    </sql>

    <sql id="cols_all">
        id,
        <include refid="cols_exclude_id"/>
    </sql>

    <sql id="cols_exclude_id">
        year, month,`status`, order_count, `order_fee`, order_member, visitor_json, category_json, extra_json, created_at, updated_at
    </sql>

    <sql id="vals">
        #{year}, #{month}, #{status}, #{orderCount}, #{orderFee}, #{orderMember}, #{visitorJson}, #{categoryJson}, #{extraJson}, now(), now()
    </sql>

    <sql id="queryCondition">
        where 1 = 1
        <if test="year != null">and `year` = #{year}</if>
        <if test="month != null">and `month` = #{month}</if>
        <if test="status != null">and `status` = #{status}</if>
        <if test="startAt != null">and created_at &gt; #{startAt}</if>
        <if test="endAt != null">and created_at &lt; #{endAt}</if>
    </sql>

    <sql id="order">
        order by id desc
    </sql>


    <insert id="create" parameterType="VegaReportMonthSale" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb"/>
        (<include refid="cols_exclude_id"/>)
        VALUES
        (<include refid="vals"/>)
    </insert>

    <select id="findById" parameterType="long" resultMap="VegaReportMonthSaleMap">
        SELECT
        <include refid="cols_all"/>
        FROM
        <include refid="tb"/>
        WHERE id = #{id}
    </select>

    <select id="findByIds" parameterType="list" resultMap="VegaReportMonthSaleMap">
        select id,
        <include refid="cols_exclude_id"/>
        from
        <include refid="tb"/>
        where id in
        <foreach collection="list" open="(" separator="," close=")"
                 item="item">
            #{item}
        </foreach>
        <include refid="order"/>
    </select>

    <select id="findByYear" parameterType="integer" resultMap="VegaReportMonthSaleMap">
        SELECT
        <include refid="cols_all"/>
        FROM
        <include refid="tb"/>
        WHERE year = #{year}
    </select>

    <select id="findByYearAndMonth" parameterType="VegaReportMonthSale" resultMap="VegaReportMonthSaleMap">
        SELECT
        <include refid="cols_all"/>
        FROM
        <include refid="tb"/>
        WHERE year = #{year} and month = #{month}
    </select>

    <update id="update" parameterType="VegaReportMonthSale">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="status != null">`status` = #{status},</if>
            <if test="orderCount != null">order_count = #{orderCount},</if>
            <if test="orderFee != null">order_fee = #{orderFee},</if>
            <if test="orderMember != null">order_member = #{orderMember},</if>
            <if test="visitorJson != null">visitor_json = #{visitorJson},</if>
            <if test="categoryJson != null">category_json = #{categoryJson},</if>
            <if test="extraJson != null">extra_json = #{extraJson},</if>
            updated_at=now()
        </set>
        WHERE id = #{Id}
    </update>


    <update id="updateByYearAndMonth" parameterType="VegaReportMonthSale">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="status != null">`status` = #{status},</if>
            <if test="orderCount != null">order_count = #{orderCount},</if>
            <if test="orderFee != null">order_fee = #{orderFee},</if>
            <if test="orderMember != null">order_member = #{orderMember},</if>
            <if test="visitorJson != null">visitor_json = #{visitorJson},</if>
            <if test="categoryJson != null">category_json = #{categoryJson},</if>
            <if test="extraJson != null">extra_json = #{extraJson},</if>
            updated_at=now()
        </set>
        WHERE year = #{year} and month = #{month}
    </update>

    <select id="paging" parameterType="list" resultMap="VegaReportMonthSaleMap">
        select id,
        <include refid="cols_exclude_id"/>
        from
        <include refid="tb"/>
        <include refid="queryCondition"/>
        order by id desc limit #{offset},#{limit}
    </select>

    <select id="count" parameterType="list" resultType="long">
        select count(1)
        from
        <include refid="tb"/>
        <include refid="queryCondition"/>
    </select>
</mapper>