<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="OrderDispatchRelation">
    <resultMap id="OrderDispatchRelationMap" type="OrderDispatchRelation">
        <id column="id" property="id"/>
        <result column="operate_id" property="operateId"/>
        <result column="operate_name" property="operateName"/>
        <result column="order_id" property="orderId"/>
        <result column="receive_shop_id" property="receiveShopId"/>
        <result column="dispatch_shop_id" property="dispatchShopId"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>

    </resultMap>

    <sql id="tb">
        vega_order_dispatch_relation
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        operate_id, operate_name, order_id, receive_shop_id, dispatch_shop_id, created_at, updated_at
    </sql>

    <sql id="vals">
        #{operateId}, #{operateName}, #{orderId}, #{receiveShopId}, #{dispatchShopId}, now(), now()
    </sql>

    <sql id="queryCondition">
        where 1 = 1

        <if test="operateId != null">and `operate_id` = #{operateId}</if>

        <if test="operateName != null">and `operate_name` = #{operateName}</if>

        <if test="orderId != null">and `order_id` = #{orderId}</if>

        <if test="receiveShopId != null">and `receive_shop_id` = #{receiveShopId}</if>

        <if test="dispatchShopId != null">and `dispatch_shop_id` = #{dispatchShopId}</if>

        <if test="createdAt != null">and `created_at` = #{createdAt}</if>

        <if test="updatedAt != null">and `updated_at` = #{updatedAt}</if>

        <if test="startAt != null">and created_at &gt; #{startAt}</if>
        <if test="endAt != null">and created_at &lt; #{endAt}</if>
    </sql>

    <sql id="order">
        order by id desc
    </sql>

    <insert id="create" parameterType="OrderDispatchRelation" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="findById" parameterType="long" resultMap="OrderDispatchRelationMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>


    <select id="findByOrderIdAndDispatchShopId" parameterType="map" resultMap="OrderDispatchRelationMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE order_id = #{orderId} AND dispatch_shop_id =#{dispatchShopId}
    </select>



   <select id="findByIds" parameterType="list" resultMap="OrderDispatchRelationMap">
        select id, <include refid="cols_exclude_id"/>
        from <include refid="tb"/>
        where id in
        <foreach collection="list" open="(" separator="," close=")"
                 item="item">
            #{item}
        </foreach>
        <include refid="order" />
    </select>

    <update id="update" parameterType="OrderDispatchRelation">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="operateId != null">operate_id = #{operateId},</if>
            <if test="operateName != null">operate_name = #{operateName},</if>
            <if test="orderId != null">order_id = #{orderId},</if>
            <if test="receiveShopId != null">receive_shop_id = #{receiveShopId},</if>
            <if test="dispatchShopId != null">dispatch_shop_id = #{dispatchShopId},</if>



            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <select id="paging" parameterType="list" resultMap="OrderDispatchRelationMap">
        select id, <include refid="cols_exclude_id"/>
        from <include refid="tb"/>
        <include refid="queryCondition"/>
        order by id desc limit #{offset},#{limit}
    </select>

    <select id="count" parameterType="list" resultType="long">
        select count(1)
        from <include refid="tb"/>
        <include refid="queryCondition"/>
    </select>

</mapper>