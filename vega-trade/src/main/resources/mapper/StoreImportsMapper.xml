<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="StoreImports">
    <resultMap id="StoreImportsMap" type="StoreImports">
        <id column="id" property="id"/>
        <result column="batch_no" property="batchNo"/>
        <result column="son_batch_no" property="sonBatchNo"/>
        <result column="user_id" property="userId"/>
        <result column="user_name" property="userName"/>
        <result column="status" property="status"/>
        <result column="type" property="type"/>
        <result column="result" property="result"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="tb">
        vega_store_imports
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        batch_no, son_batch_no, user_id, user_name,status, type, result, created_at, updated_at
    </sql>

    <sql id="vals">
        #{batchNo}, #{sonBatchNo}, #{userId}, #{userName}, #{status}, #{type}, #{result}, now(), now()
    </sql>

    <sql id="queryCondition">
        where 1 = 1

        <if test="batchNo != null">and `batch_no` = #{batchNo}</if>

        <if test="sonBatchNo != null">and `son_batch_no` = #{sonBatchNo}</if>

        <if test="userId != null">and `user_id` = #{userId}</if>

        <if test="userName != null">and `user_name` = #{userName}</if>

        <if test="status != null">and `status` = #{status}</if>

        <if test="type != null">and `type` = #{type}</if>

        <if test="result != null">and `result` = #{result}</if>

        <if test="createdAt != null">and `created_at` = #{createdAt}</if>

        <if test="updatedAt != null">and `updated_at` = #{updatedAt}</if>

        <if test="startAt != null">and created_at &gt; #{startAt}</if>

        <if test="endAt != null">and created_at &lt; #{endAt}</if>
    </sql>

    <sql id="order">
        order by id desc
    </sql>

    <insert id="create" parameterType="StoreImports" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <update id="update" parameterType="StoreImports">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="status != null">status = #{status},</if>
            <if test="result != null">result = #{result},</if>
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <select id="findById" parameterType="long" resultMap="StoreImportsMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

    <select id="findByKey" parameterType="string" resultMap="StoreImportsMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE batch_no = #{batchNo}
    </select>

    <select id="findByIds" parameterType="list" resultMap="StoreImportsMap">
        select id, <include refid="cols_exclude_id"/>
        from <include refid="tb"/>
        where id in
        <foreach collection="list" open="(" separator="," close=")"
                 item="item">
            #{item}
        </foreach>
        <include refid="order" />
    </select>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <select id="paging" parameterType="map" resultMap="StoreImportsMap">
        select id, <include refid="cols_exclude_id"/>
        from <include refid="tb"/>
        <include refid="queryCondition"/>
        order by id desc limit #{offset},#{limit}
    </select>

    <select id="count" parameterType="map" resultType="long">
        select count(1)
        from <include refid="tb"/>
        <include refid="queryCondition"/>
    </select>

</mapper>