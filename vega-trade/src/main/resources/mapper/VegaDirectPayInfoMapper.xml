<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="VegaDirectPayInfo">

    <resultMap id="VegaDirectPayInfoMap" type="VegaDirectPayInfo">
        <id column="id" property="id"/>
        <result column="order_id" property="orderId"/>
        <result column="business_id" property="businessId"/>
        <result column="describe" property="describe"/>
        <result column="status" property="status"/>
        <result column="extra_json" property="extraJson"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="tb">
        vega_direct_pay_info
    </sql>

    <sql id="cols_all">
        id,
        <include refid="cols_exclude_id"/>
    </sql>

    <sql id="cols_exclude_id">
        order_id, business_id,`describe`, status, extra_json, created_at, updated_at
    </sql>

    <sql id="vals">
        #{orderId}, #{businessId},#{describe}, #{status}, #{extraJson},now(), now()
    </sql>

    <sql id="queryCondition">
        where 1 = 1
        <if test="orderId != null">and `order_id` = #{orderId}</if>
        <if test="businessId != null">and `business_id` = #{businessId}</if>
        <if test="describe != null">and `describe` = #{describe}</if>
        <if test="status != null">and `status` = #{status}</if>
        <if test="startAt != null">and created_at &gt; #{startAt}</if>
        <if test="endAt != null">and created_at &lt; #{endAt}</if>
    </sql>

    <sql id="order">
        order by id desc
    </sql>


    <insert id="create" parameterType="VegaDirectPayInfo" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb"/>
        (<include refid="cols_exclude_id"/>)
        VALUES
        (<include refid="vals"/>)
    </insert>


    <select id="findById" parameterType="long" resultMap="VegaDirectPayInfoMap">
        SELECT
        <include refid="cols_all"/>
        FROM
        <include refid="tb"/>
        WHERE id = #{id}
    </select>

    <select id="findByBusinessId" parameterType="map" resultMap="VegaDirectPayInfoMap">
        SELECT
        <include refid="cols_all"/>
        FROM
        <include refid="tb"/>
        WHERE business_id = #{businessId}
    </select>

    <select id="findByOrderId" parameterType="map" resultMap="VegaDirectPayInfoMap">
        SELECT
        <include refid="cols_all"/>
        FROM
        <include refid="tb"/>
        WHERE order_id = #{orderId} AND status &gt; 0
    </select>


    <select id="findManyByStatus" parameterType="long" resultMap="VegaDirectPayInfoMap">
        SELECT
        <include refid="cols_all"/>
        FROM
        <include refid="tb"/>
        WHERE status = #{status}
    </select>


    <select id="findByIds" parameterType="list" resultMap="VegaDirectPayInfoMap">
        select id,
        <include refid="cols_exclude_id"/>
        from
        <include refid="tb"/>
        where id in
        <foreach collection="list" open="(" separator="," close=")"
                 item="item">
            #{item}
        </foreach>
        <include refid="order"/>
    </select>

    <update id="update" parameterType="VegaDirectPayInfo">
        UPDATE
        <include refid="tb"/>
        <set>

            <if test="describe != null">`describe` = #{describe},</if>
            <if test="status != null">status = #{status},</if>
            <if test="extraJson != null">extra_json = #{extraJson},</if>

            updated_at=now()
        </set>
        WHERE business_id = #{businessId}
    </update>


    <update id="updateStatusByBusinessId" parameterType="map">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="newStatus != null">status = #{newStatus},</if>
            updated_at=now()
        </set>
        WHERE business_id = #{businessId}
    </update>


    <delete id="deleteByBusinessId" parameterType="map">
        DELETE FROM
        <include refid="tb"/>
        WHERE business_id = #{businessId}
    </delete>

    <select id="paging" parameterType="list" resultMap="VegaDirectPayInfoMap">
        select id,
        <include refid="cols_exclude_id"/>
        from
        <include refid="tb"/>
        <include refid="queryCondition"/>
        order by id desc limit #{offset},#{limit}
    </select>

    <select id="count" parameterType="list" resultType="long">
        select count(1)
        from
        <include refid="tb"/>
        <include refid="queryCondition"/>
    </select>
</mapper>