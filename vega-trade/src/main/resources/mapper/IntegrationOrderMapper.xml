<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright (c) 2016 杭州端点网络科技有限公司, Code generated by terminus code gen
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="IntegrationOrder">
    <resultMap id="IntegrationOrderMap" type="IntegrationOrder">
        <id column="id" property="id"/>
        <result column="buyer_id" property="buyerId"/>
        <result column="buyer_name" property="buyerName"/>
        <result column="buyer_phone" property="buyerPhone"/>
        <result column="item_id" property="itemId"/>
        <result column="item_name" property="itemName"/>
        <result column="item_image" property="itemImage"/>
        <result column="integration_price" property="integrationPrice"/>
        <result column="integration_fee" property="integrationFee"/>
        <result column="quantity" property="quantity"/>
        <result column="status" property="status"/>
        <result column="address_info_json" property="addressInfoJson"/>
        <result column="delivery_company" property="deliveryCompany"/>
        <result column="delivery_no" property="deliveryNo"/>
        <result column="extra_json" property="extraJson"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        
    </resultMap>

    <sql id="tb">
        vega_integration_orders
    </sql>

    <sql id="cols_all">
        id, <include refid="cols_exclude_id" />
    </sql>

    <sql id="cols_exclude_id">
        buyer_id, buyer_name, buyer_phone, item_id, item_name, item_image, integration_price, integration_fee, quantity, status, address_info_json, delivery_company, delivery_no, extra_json, created_at, updated_at
    </sql>

    <sql id="vals">
        #{buyerId}, #{buyerName}, #{buyerPhone}, #{itemId}, #{itemName}, #{itemImage}, #{integrationPrice}, #{integrationFee}, #{quantity}, #{status}, #{addressInfoJson}, #{deliveryCompany}, #{deliveryNo}, #{extraJson}, now(), now()
    </sql>

    <sql id="queryCondition">
        where 1 = 1

        <if test="id != null">and `id` = #{id}</if>

        <if test="buyerId != null">and `buyer_id` = #{buyerId}</if>

        <if test="buyerName != null">and `buyer_name` = #{buyerName}</if>
        
        <if test="buyerPhone != null">and `buyer_phone` = #{buyerPhone}</if>

        <if test="status != null">and `status` = #{status}</if>

        <if test="createdAt != null">and `created_at` = #{createdAt}</if>

        <if test="updatedAt != null">and `updated_at` = #{updatedAt}</if>
        
        <if test="startAt != null">and created_at &gt; #{startAt}</if>
        <if test="endAt != null">and created_at &lt; #{endAt}</if>
    </sql>

    <sql id="order">
        order by id desc
    </sql>

    <insert id="create" parameterType="IntegrationOrder" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO
        <include refid="tb" />
        (<include refid="cols_exclude_id" />)
        VALUES
        (<include refid="vals" />)
    </insert>

    <select id="findById" parameterType="long" resultMap="IntegrationOrderMap" >
        SELECT <include refid="cols_all" />
        FROM <include refid="tb" />
        WHERE id = #{id}
    </select>

   <select id="findByIds" parameterType="list" resultMap="IntegrationOrderMap">
        select id, <include refid="cols_exclude_id"/>
        from <include refid="tb"/>
        where id in
        <foreach collection="list" open="(" separator="," close=")"
                 item="item">
            #{item}
        </foreach>
        <include refid="order" />
    </select>

    <update id="update" parameterType="IntegrationOrder">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="buyerId != null">buyer_id = #{buyerId},</if>
            <if test="buyerName != null">buyer_name = #{buyerName},</if>
            <if test="buyerPhone != null">buyer_phone = #{buyerPhone},</if>
            <if test="itemId != null">item_id = #{itemId},</if>
            <if test="itemName != null">item_name = #{itemName},</if>
            <if test="itemImage != null">item_image = #{itemImage},</if>
            <if test="integrationPrice != null">integration_price = #{integrationPrice},</if>
            <if test="integrationFee != null">integration_fee = #{integrationFee},</if>
            <if test="quantity != null">quantity = #{quantity},</if>
            <if test="status != null">status = #{status},</if>
            <if test="addressInfoJson != null">address_info_json = #{addressInfoJson},</if>
            <if test="deliveryCompany != null">delivery_company = #{deliveryCompany},</if>
            <if test="deliveryNo != null">delivery_no = #{deliveryNo},</if>
            <if test="extraJson != null">extra_json = #{extraJson},</if>
            
            
            
            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <update id="delivery" parameterType="IntegrationOrder">
        UPDATE
        <include refid="tb"/>
        <set>
            <if test="deliveryCompany != null">delivery_company = #{deliveryCompany},</if>
            <if test="deliveryNo != null">delivery_no = #{deliveryNo},</if>
            <if test="status != null">status = #{status},</if>


            updated_at=now()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM <include refid="tb"/>
        WHERE id = #{id}
    </delete>

    <select id="paging" parameterType="list" resultMap="IntegrationOrderMap">
        select id, <include refid="cols_exclude_id"/>
        from <include refid="tb"/>
        <include refid="queryCondition"/>
        order by id desc limit #{offset},#{limit}
    </select>

    <select id="count" parameterType="list" resultType="long">
        select count(1)
        from <include refid="tb"/>
        <include refid="queryCondition"/>
    </select>

</mapper>